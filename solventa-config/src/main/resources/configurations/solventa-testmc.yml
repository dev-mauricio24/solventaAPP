server:
  port: 8090

spring:
  application:
    name: solventa-testmc

  datasource:
    url: jdbc:mysql://localhost:3306/studentDB
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Hilm2102

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

server:
  port: 8761

spring:
  application:
    name: solventa-eureka
eureka:
  instance:
    hostname: localhost
    client:
      register-with-eureka: false
      fetch-registry: false
      server-url:
        default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/